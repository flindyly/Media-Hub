import {videoTypes} from "@/predefined/videoTypes";
import {imageTypes} from "@/predefined/imageTypes";
import {animationTypes} from "@/predefined/animationTypes";
import {interactiveTypes} from "@/predefined/interactiveTypes";

export const mediaTypes = {
    'Video': videoTypes,
    'Image': imageTypes,
    'Animation': animationTypes,
    'Interactive': interactiveTypes
};